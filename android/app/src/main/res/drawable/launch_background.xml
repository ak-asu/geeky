<?xml version="1.0" encoding="utf-8"?>
<!-- Legacy splash (pre-Android 12): plain dark background.
     Android 12+ devices use windowSplashScreenBackground in values-v31/styles.xml
     and the OS renders ic_launcher with proper squircle masking — no square patch. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
</layer-list>
